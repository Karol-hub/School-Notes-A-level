bi) 
Turns high level code into machine code.
bii)
- Compiler creates an executable file specific to a system architecture, which is faster to run than in interpreter but creating the exe file is slower than running an interpreter
- Interpreter goes line by line, so it can flag an error as it encounters it, Compiler goes through all the code at once, meaning it can't flag an error as it passes it
